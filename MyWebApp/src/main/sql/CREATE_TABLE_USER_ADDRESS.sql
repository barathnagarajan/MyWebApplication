CREATE TABLE TEST.USER_ADDRESS(ADDRESS_ID NUMERIC NOT NULL, USER_ID NUMERIC NOT NULL REFERENCES TEST.MJ_USERS(USER_ID),
DOOR_NO NUMERIC NOT NULL, STREET_NAME VARCHAR(100), AREA VARCHAR(25), COUNTRY VARCHAR(20), PIN_CODE NUMERIC, MOBILE_NUMBER NUMERIC, LANDLINE numeric);